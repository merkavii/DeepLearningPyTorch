# * ==============================================================================
# *                          DVC چیست و چه مشکلی را حل می‌کند؟
# * ==============================================================================
# ? چالش اصلی: گیت  برای فایل‌های سورس‌کد عالیه، اما اگه فایل‌های چندصد مگابایتی
# ? یا چند گیگابایتی (مثل دیتابیس تیکت‌ها یا تصاویر) رو توی گیت ذخیره کنی، رپو قفل می‌کنه!
# * راهکار DVC: این ابزار فایل‌های سنگین رو نگه می‌داره و به جاش فقط یک فایل متنی
# * بسیار سبک با پسوند .dvc می‌سازه که اطلاعات هش اون فایل داخلشه.
# * ما فقط همین فایل .dvc رو به Git می‌دیم!

# * ==============================================================================
# *                          ۱. راه‌اندازی اولیه DVC
# * ==============================================================================
# ? مقداردهی اولیه DVC در پوشه پروژه (مشابه git init)
# todo dvc init

# ? ثبت تغییرات اولیه ساختار DVC در کنترل نسخه Git
# todo git add .dvc .gitignore
# todo git commit -m "Initialize DVC in project"

# * ==============================================================================
# *                 ۲. اضافه کردن فایل داده به کنترل DVC
# * ==============================================================================
# ? اضافه کردن فایل دیتابیس سنگین تیکت‌های پشتیبانی به DVC
# todo dvc add data/tickets_v1.csv

# ! مهم: DVC اتوماتیک یک فایل کوچک data/tickets_v1.csv.dvc می‌سازد
# ! و اسم data/tickets_v1.csv را به .gitignore اضافه می‌کند تا Git آن را آپلود نکند.

# * ==============================================================================
# *                 ۳. ثبت فایل متنی .dvc در Git (نسخه‌بندی داده)
# * ==============================================================================
# ? حالا فقط فایل متنی کوچک .dvc و فایل .gitignore به Git سپرده می‌شوند
# todo git add data/tickets_v1.csv.dvc data/.gitignore
# todo git commit -m "add: track tickets_v1.csv via DVC"

# * ==============================================================================
# *                 ۴. تعریف Storage مقصد (Remote Storage)
# * ==============================================================================
# ? معرفی سرور یا پوشه مقصد جهت آپلود فایل‌های واقعی (S3، سرور محلی یا غیره)
# todo: تغییر آدرس مسیر زیر به آدرس سرور یا Storage واقعی شما
# todo dvc remote add -d my_remote /path/to/remote/storage

# ? ثبت تنظیمات Remote Storage در فایل کانفیگ Git
# todo git add .dvc/config
# todo git commit -m "configure DVC remote storage"

# * ==============================================================================
# *                 ۵. ارسال فایل اصلی به Storage
# * ==============================================================================
# ? آپلود اصل فایل‌های سنگین به Storage تعیین شده (مشابه git push)
# todo dvc push

# * ==============================================================================
# *                 ۶. دریافت داده در سیستم جدید یا توسط همکار
# * ==============================================================================
# ? ۱. دریافت آخرین کدهای پایتون و فایل‌های کوچک .dvc از مخزن Git
# // git pull

# ? ۲. دانلود فایل‌های سنگین متناظر با آن Commit از DVC Storage
# // dvc pull

# * ==============================================================================
# *                 ۷. سوئیچ کردن بین نسخه‌های مختلف دیتاست
# * ==============================================================================
# ? ۱. بازگشت به Commit قبلی در Git برای نسخه قدیمی‌تر دیتاست
# // git checkout <commit_hash_v1>

# ? ۲. همگام‌سازی فایل‌های سنگین روی هارد متناظر با همان Commit
# // dvc checkout
"""
====================================================================================================
🧠 شهود ریاضی پشت صحنه: FAISS چطور کار می‌کند؟ (به زبان بسیار ساده)
====================================================================================================

* فرض کن در یک اتاق بزرگ، ۱,۰۰۰ کفش و لباس مختلف داریم.
* هر محصول را می‌توان با ۲ ویژگی توصیف کرد: 
*   1. رنگ (از سفید تا مشکی) 
*   2. نوع (از کاملاً ورزشی تا کاملاً رسمی)

* اگر این دو ویژگی را به صورت عددی بین 0 تا 1 نشان دهیم، هر محصول یک نقطه روی صفحه دو بعدی (x, y) می‌شود:
*   - کفش رسمی مشکی  <---  (0.1, 0.9)
*   - کفش ورزشی سفید  <---  (0.9, 0.1)

* حالا وقتی کاربر عکس یک کفش ورزشی سفید جدید را آپلود می‌کند، مدل ما آن را به نقطه (0.85, 0.15) تبدیل می‌کند.
* وظیفه اصلی FAISS محاسبه سریع فاصله ریاضی (اقلیدسی یا کسینوسی) بین این نقطه جدید و تمام نقاط دیتابیس است 
* تا نزدیک‌ترین محصولات را در کسری از ثانیه پیدا کند.

* در پروژه واقعی ما به جای ۲ ویژگی، ۵۱۲ ویژگی (یک فضای ۵۱۲ بعدی!) داریم.
* محاسبه فاصله در فضای ۵۱۲ بعدی کار فوق‌العاده سنگینی است، اما تخصص اصلی و خارق‌العاده FAISS همین است!
====================================================================================================
"""

# ! ابتدا کتابخانه FAISS را با دستور زیر نصب کن (نسخه CPU برای توسعه اولیه عالی است):
# ! pip install faiss-cpu

import numpy as np
import faiss

# * ۱. تعیین ابعاد بردارها (مدل CLIP ما بردارهای ۵۱۲ تایی تولید می‌کند)
dimension = 512 

# * ۲. ساخت یک ایندکس ساده بر پایه فاصله اقلیدسی (L2 Distance)
# * کلمه Flat یعنی بردارها به صورت مستقیم و بدون فشرده‌سازی در رم ذخیره شوند (دقیق‌ترین حالت)
index = faiss.IndexFlatL2(dimension)
print(f"Is index trained? {index.is_trained}") # خروجی True است، چون ایندکس ساده نیاز به آموزش ندارد.

# * ۳. شبیه‌سازی بردارهای تصاویر دیتابیس ما (فرض کن ۵ تصویر در دیتابیس داریم)
# ! نکته خیلی مهم: FAISS فقط و فقط با آرایه‌های Float32 در کتابخانه NumPy کار می‌کند!
vectors_in_db = np.random.random((5, dimension)).astype('float32')

# * ۴. اضافه کردن بردارها به دیتابیس برداری
index.add(vectors_in_db)
print(f"Total vectors in index: {index.ntotal}") # خروجی باید 5 باشد.

# * ۵. شبیه‌سازی تصویر جدیدی که کاربر آپلود کرده است (یک تک بردار ۵۱۲ تایی)
query_vector = np.random.random((1, dimension)).astype('float32')

# * ۶. جستجوی نزدیک‌ترین همسایه‌ها (k=2 یعنی ۲ تا از شبیه‌ترین تصاویر را می‌خواهیم)
k = 2
distances, indices = index.search(query_vector, k)

# * ۷. نتایج
# ? آرایه indices به ما شماره ردیف (ID) شبیه‌ترین تصاویر در دیتابیس را می‌دهد
print(f"Nearest image IDs: {indices}") 
# ? آرایه distances میزان فاصله (هر چه کمتر، شبیه‌تر) را به ما نشان می‌دهد
print(f"Distances to query: {distances}")